name: Daily Predict (Telegram)

on:
  schedule:
    - cron: "00 06 * * *"   # 06:00 UTC — après Online Update & (éventuel) Retrain
  workflow_dispatch: {}

permissions:
  contents: write   # nécessaire pour committer data/preds_log.csv

jobs:
  predict:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 1) Priorité aux modèles "online" produits par daily_online_update.yml
      - name: Try download online models
        continue-on-error: true
        uses: dawidd6/action-download-artifact@v6
        with:
          workflow: daily_online_update.yml
          workflow_conclusion: success
          name: trained-models-online
          path: .

      # 2) Si aucun modèle online n'est présent, fallback vers les modèles du retrain
      - name: If no models, fallback to retrain artifacts
        if: ${{ !hashFiles('models/*_model.pkl') }}
        uses: dawidd6/action-download-artifact@v6
        with:
          workflow: daily_retrain.yml
          workflow_conclusion: success
          name: trained-models
          path: .

      - name: Show models content (debug)
        run: |
          echo "== Repo root =="
          ls -lah
          echo "== models/ =="
          ls -lah models || true

      - name: Run daily prediction (sends Telegram)
        env:
          EXCHANGE: ${{ vars.EXCHANGE || 'kucoin' }}
          SYMBOLS: ${{ vars.SYMBOLS || 'BTC/USDT,ETH/USDT,SOL/USDT' }}
          TIMEFRAME: ${{ vars.TIMEFRAME || '4h' }}
          LIMIT: ${{ vars.LIMIT || '5000' }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python -m src.predict

      # Versionne uniquement le journal des prédictions
      - name: Commit & push preds_log.csv
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          mkdir -p data
          git add data/preds_log.csv || true
          git commit -m "Append daily prediction to preds_log.csv" || echo "Nothing to commit"
          git push

      # (Optionnel) Conserve aussi le log en artifact pour debug
      - name: Upload preds log artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: preds-log
          path: data/preds_log.csv
          if-no-files-found: warn
